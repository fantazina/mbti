import React, { useState } from 'react';

const Start = (props) => {
    const { nextId } = props

    const [btnDis, setBtnDis] = useState(false)

    return (
        <div>
            
        </div>
    );
};

export default Start;